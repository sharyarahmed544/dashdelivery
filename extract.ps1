Add-Type -AssemblyName System.IO.Compression.FileSystem; $zip = [System.IO.Compression.ZipFile]::OpenRead("c:\Users\shahe\OneDrive\Documents\dashdelivery\dash-antigravity-brief.docx"); $entry = $zip.GetEntry("word/document.xml"); $stream = $entry.Open(); $reader = New-Object System.IO.StreamReader($stream); $xmlStr = $reader.ReadToEnd(); $reader.Close(); $zip.Dispose(); $xml = [xml]$xmlStr; $xml.SelectNodes("//*[local-name()='t']") | ForEach-Object { $_."#text" }
